#ifndef Apis_h
#define Apis_h

#include "WString.h"

#endif

//create table app(id, name, author, installDate)
//create table api(id, name, order, appId)
//create table api_input(id, name, apiId)
//create table lua_codes(id, code, appid , stup_part, loop_part, other_part)
//create table devices(id, type, ip, reserved)
//create table log(id, title, description, appId, apiId, )

//create table switch(id, deviceId, name)
//create table (id, )
//create table (id, )
//